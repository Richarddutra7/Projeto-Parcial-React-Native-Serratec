import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    titulo: {
        marginTop: 16,
        color: '#000',
        fontSize: 16,
        textAlign: "center"
    }
});

export default styles;