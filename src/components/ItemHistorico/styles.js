import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    data: {
        fontSize: 14,
        fontWeight: '500',
        marginBottom: 4,
        color: '#888'
    }
});

export default styles;