import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    containerCards: {
        height: 132,
        marginTop: 8,
        marginBottom: '8%'
    }
});

export default styles;